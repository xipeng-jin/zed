[package]
name = "browser"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"

[lints]
workspace = true

[lib]
path = "src/browser.rs"
doctest = false

[features]
# The real CEF engine. Off by default so the crate's deterministic tests build
# and run with no CEF distribution present (ticket #8); `crates/zed` turns it
# on for the shipped binary.
cef = ["dep:cef", "dep:glib-sys"]
# Exposes the scripted stub tab backend and the message-pump simulation for
# tests in dependent crates.
test-support = []

[dependencies]
anyhow.workspace = true
db.workspace = true
editor.workspace = true
fuzzy.workspace = true
gpui.workspace = true
image.workspace = true
log.workspace = true
menu.workspace = true
notifications.workspace = true
parking_lot.workspace = true
paths.workspace = true
project.workspace = true
serde.workspace = true
serde_json.workspace = true
smallvec.workspace = true
ui.workspace = true
url.workspace = true
util.workspace = true
workspace.workspace = true
zed_actions.workspace = true

[target.'cfg(any(target_os = "linux", target_os = "macos", target_os = "windows"))'.dependencies]
cef = { workspace = true, optional = true }

[target.'cfg(target_os = "linux")'.dependencies]
# The message pump must iterate the default GMainContext on Linux: Chromium's
# X11 event source attaches there, and native popup windows go input-dead
# without it (see `start_message_pump`). Already in the dependency tree via
# libwebrtc.
glib-sys = { version = "0.21", optional = true }

[dev-dependencies]
db = { workspace = true, features = ["test-support"] }
tempfile.workspace = true
editor = { workspace = true, features = ["test-support"] }
gpui = { workspace = true, features = ["test-support"] }
project = { workspace = true, features = ["test-support"] }
workspace = { workspace = true, features = ["test-support"] }
